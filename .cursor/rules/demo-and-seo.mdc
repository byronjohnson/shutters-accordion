---
description: Marketing site moved to shutters-site — see site-html-seo rule there
globs: demo/**
alwaysApply: false
---

# Demo & SEO (migrated)

The in-repo `demo/` folder was removed. **Demo, documentation, and SEO** live at:

- **Site:** https://shuttersjs.com/
- **Repo:** sibling `shutters-site` (Vercel)

Edit marketing HTML, JSON-LD, and page zones in **shutters-site** — use that repo's `.cursor/rules/site-html-seo.mdc` and `update-seo` skill.

## Legacy URL

https://byronjohnson.github.io/shutters-accordion/demo/ redirects to https://shuttersjs.com/ via `deploy-demo.yml`.

## Library releases

After changing `src/` library files, release to npm then update shutters-site dependency — see shutters-site `sync-library-release` skill.
